---
const { link, text } = Astro.props;
---

<article class="knapper">
  <a href={link} class="knap-item">{text}</a>
</article>

<style>
  .knapper {
    padding: 25px;
    border: 1px solid rgb(255, 126, 34);
    border-radius: 10px;
  }
  .knap-item {
    list-style: none;
    text-decoration: none;
    color: rgb(255, 126, 34);
    font-size: 1.5rem;
  }
  .knapper:hover {
    box-shadow: 0 0 10px rgb(255, 126, 34);
    color: white;
  }
  .knap-item {
    position: relative;
  }

  /* Bølge-effekten */
  .knap-item::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: rgb(255, 126, 34);
    transform: scaleX(0);
    transform-origin: bottom right;
    transition: transform 0.3s ease-out;
  }
  /* Når musen hover over menupunktet */
  .knap-item:hover::after {
    transform: scaleX(1);
    transform-origin: bottom left;
  }
</style>
